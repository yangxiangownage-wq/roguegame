{
  "name": "rogue-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "RogueGame — 1920×1080 letterbox canvas",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "verify:fit": "node scripts/verify-fit.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
